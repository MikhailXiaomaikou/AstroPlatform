# Core infrastructure tools — loaded permanently regardless of focus.
# Descriptions in ai_tools.py MUST stay domain-neutral for these tools.

tools:
  - astro_statistics_toolbox
  - crossmatch_catalogs
  - describe_tap_table
  - extract_literature_tables
  - get_extinction
  - get_last_search_results
  - get_object_dossier
  - get_object_info
  - read_arxiv_paper
  - run_adql
  - run_python
  - run_sdss_sql
  - search_literature
  - search_objects
  - sensitivity_analysis
  # Cross-cutting async-job polling (P1.2.d, 2026-05-22):
  # Any focus may submit long tools via async_tool_runtime; this is the
  # generic poll entry. Domain-neutral on purpose.
  - get_async_job_status
